<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.css" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="container">
    <header>
        <h1 class="display-4"><%= title %></h1>
    </header>
    <div role="main">
      <% if (userName) { %>
        <p class="h4"><%= userName %>'s messages.</p>
      <% } %>
      <table class="table mt-5">
        <% content.forEach(item => { %>
            <%- include("data_item", { val: item }) %>
        <% }) %>
      </table>

      <ul class="pagination justify-content-center">
        <li class="page-item <%= page <= 0 ? 'disabled' : '' %>">
            <a href="/boards/home/<%= userName %>/<%= accountId %>/<%= page-1 %>" class="page-link">&lt;&lt; prev</a>
        </li>
        <li class="page-item">
            <span class="page-link"><%= page + 1 %></span>
        </li>
        <li class="page-item">
            <a href="/boards/home/<%= userName %>/<%= accountId %>/<%= page+1 %>" class="page-link">Next &gt;&gt;</a>
        </li>
      </ul>
    </div>
    <div class="text-left">
        <a href="/boards">&lt;&lt; Top.</a>
    </div>
</body>
</html>
